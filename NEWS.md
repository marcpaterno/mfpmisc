# mfpmisc 0.1.0

* Initial release
* Added `ls.objects()` for listing objects in environment
* Added `lsos()` for listing objects by size
* Added `lspackages()` for listing installed packages
* Added `make_document()` for creating Quarto documents